# Analysis results

Each analysis run creates a subdirectory named after the experiment
identifier (see the project README for the naming convention).

`23101600_DSUP0_0/` is kept as a structural example showing the expected
output layout. It can also be used as a test to challenge an update of the
analysis pipeline. All other analysis directories are gitignored — they are
reproducible by re-running the pipeline.
